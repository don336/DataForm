import styled from "styled-components";
import { Stack } from "@mui/material";
import { COLORS } from "../../Styles/theme";

export const StyledNav = styled(Stack)`
  background: ${COLORS.CHARCOAL};
  padding: 22px;
  justify-content: space-between;
`;
